{
  "_meta": {
    "expected_behavior": "No actor exists with this id. Implementation MUST return 404 with code `not_found` and MUST NOT cache an operation row (failed-precondition operations are not cached, so a later retry with the same operation_id and a valid actor_id is allowed)."
  },
  "_error": {
    "status": 404,
    "code": "not_found",
    "details": {
      "field": "actor_id",
      "reason": "No actor with this id."
    }
  }
}
