{
  "_meta": {
    "expected_behavior": "In v0.1.3, `kind: \"agent\"` is allowed only when the request also supplies the required agent-registration fields. This fixture intentionally omits `agent_runtime` and `parent_actor_id`, so the implementation MUST reject with 400 Bad Request before performing any state mutation."
  },
  "_error": {
    "status": 400,
    "code": "validation_error",
    "details": {
      "field": "agent_runtime",
      "reason": "Agent registration requires both `agent_runtime` and `parent_actor_id`."
    }
  }
}
