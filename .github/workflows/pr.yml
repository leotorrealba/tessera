name: pr

on:
  pull_request:
    branches: [main]

jobs:
  # Docs-only repo. The `pr-title` check fires from the dedicated
  # pr-title.yml workflow; this file is a placeholder so branch protection
  # always finds a workflow file at .github/workflows/pr.yml.
  noop:
    name: noop
    runs-on: ubuntu-latest
    steps:
      - run: echo "docs-only repo; only pr-title is enforced"
