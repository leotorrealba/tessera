{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/leotorrealba/tessera/schemas/v0.1.4/verbs/attachment.finalize.req.json",
  "title": "attachment.finalize — Request",
  "description": "Confirm that binary bytes have been successfully uploaded and transition the attachment from pending to ready. Idempotent via operation_id.",
  "type": "object",
  "required": ["operation_id", "attachment_id"],
  "properties": {
    "operation_id": {
      "type": "string",
      "format": "uuid",
      "description": "UUIDv7 (time-ordered) recommended. Used for idempotency."
    },
    "attachment_id": {
      "type": "string",
      "format": "uuid",
      "description": "ID of the pending attachment to finalize."
    }
  },
  "additionalProperties": false
}
